import React, { Component } from 'react';

class login extends Component {
  // state = {users: []}

  // componentDidMount() {
  //   fetch('/users')
  //     .then(res => res.json())
  //     .then(users => this.setState({ users }));
  // }

  render() {
    return (
      <div className="login">
        <h1>Welcome to Salaryo, please login</h1>
      </div>
    );
  }
}

export default login;